private_lane :prebuild_rn_boilerplate_app_android do |options|
  environment = options[:environment]
  # https://docs.fastlane.tools/actions/sh/
  sh("npm run prebuild:#{environment} -- -p android")
end
